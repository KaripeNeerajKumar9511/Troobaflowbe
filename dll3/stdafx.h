#pragma once

#include <windows.h>
#include <iostream>
#include <fstream>
#include <string>
#include <cmath>
#include <vector>

// Forward declarations for logging helpers used across the project
void write_check(char* varlocal, char* s0);
void read_check(char* xx1);